# Data Structures From Scratch (Java)
Linked list (with iterator + reverse), array stack, circular-buffer queue, min-heap, chained hash map with
rehashing, binary search tree. Complexity noted in comments; self-checking demo in main().
Shows: generics, inner/nested classes, Iterable, recursion, hashing, amortised analysis.

Run:  javac Main.java && java Main
