# Bank System (Java OOP)
Shows: abstract classes, inheritance, interfaces, polymorphism, encapsulation, custom checked exception,
enums, records, generics (`<T extends Account>`), collections, Optional, streams.

Run:  javac Main.java && java Main
